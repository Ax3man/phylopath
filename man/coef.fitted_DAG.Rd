% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DAG.R
\name{coef.fitted_DAG}
\alias{coef.fitted_DAG}
\alias{confint.fitted_DAG}
\title{Extract the coefficients and confidence intervals of a fitted causal model.}
\usage{
\method{coef}{fitted_DAG}(object, ...)

\method{confint}{fitted_DAG}(object, parm, level = 0.95, ...)
}
\arguments{
\item{object}{An object of class \code{fitted_DAG}.}

\item{...}{Ignored, present for consistency with the generic.}

\item{parm}{The paths for which to return intervals, given as names or indices. Defaults to all
of them.}

\item{level}{The confidence level, which has to be 0.95 for now.}
}
\value{
For \code{coef()}, a named numeric vector with one element per path. For \code{confint()}, a
matrix with a row per path and the lower and upper bound in its two columns.
}
\description{
\code{coef()} returns the path coefficients as a named vector, and \code{confint()} the bounds of their
confidence intervals. Both name each path as \code{"from -> to"} and return them in the same order, so
they can be combined with \code{cbind()}. Paths that are absent from the causal model are not
included. For a data frame with the standard errors alongside the coefficients, see
\code{\link[=as.data.frame.fitted_DAG]{as.data.frame.fitted_DAG()}}.
}
\details{
Note that these intervals are bootstrap percentile intervals, collected while fitting, and not
the parametric intervals that \code{confint()} returns for most model objects.
}
\examples{
  d <- DAG(LS ~ BM, NL ~ BM, DD ~ NL + LS)
  d_fitted <- est_DAG(d, rhino, rhino_tree, 'lambda')
  coef(d_fitted)

  # Confidence intervals require the model to be fitted with bootstrapping.
  \donttest{
    d_boot <- est_DAG(d, rhino, rhino_tree, 'lambda', boot = 100)
    confint(d_boot)
    cbind(coef = coef(d_boot), confint(d_boot))
  }
}
