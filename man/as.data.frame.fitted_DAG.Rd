% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DAG.R
\name{as.data.frame.fitted_DAG}
\alias{as.data.frame.fitted_DAG}
\title{Extract the paths of a fitted causal model.}
\usage{
\method{as.data.frame}{fitted_DAG}(x, row.names = NULL, optional = FALSE, ..., order_by = "default")
}
\arguments{
\item{x}{An object of class \code{fitted_DAG}.}

\item{row.names, optional, ...}{Ignored, present for consistency with the
generic.}

\item{order_by}{Either \code{"default"}, to order the paths by their position in
the causal model, \code{"causal"}, to follow the paths downstream, or
\code{"strength"}, to order them by the size of their coefficients. Ordering by
strength is not available for models that contain both binary and continuous
variables, since their coefficients are not comparable, see \code{\link[=est_DAG]{est_DAG()}}.
Ordering by cause is not available for cyclical models, which \code{\link[=average]{average()}} can
produce when the direction of a path is not resolved.}
}
\value{
A \code{data.frame} with columns \code{from}, \code{to}, \code{coef} and \code{se}, as well as
\code{lower} and \code{upper} if the model was fitted with \code{boot} larger than 0.
}
\description{
Returns the estimated paths of a \code{fitted_DAG} as a long \code{data.frame}, one row
per path, which is a convenient starting point for custom tables and figures.
Paths that are absent from the causal model are not included.
}
\examples{
  d <- DAG(LS ~ BM, NL ~ BM, DD ~ NL + LS)
  d_fitted <- est_DAG(d, rhino, rhino_tree, 'lambda')
  as.data.frame(d_fitted)
  as.data.frame(d_fitted, order_by = 'strength')
}
